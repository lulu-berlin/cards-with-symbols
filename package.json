{
  "name": "cards-with-symbols",
  "version": "1.0.0",
  "description": "Generate all possible cards with a pool of n cards, such that every pair of cards has one and only one symbol in common",
  "main": "src/index.ts",
  "scripts": {
    "test": "mocha --opts mocha.opts",
    "test:auto": "nodemon --exec \"npm test\""
  },
  "author": "Ya'ar Hever",
  "license": "Apache-2",
  "dependencies": {
    "@types/chai": "^4.0.10",
    "@types/mocha": "^2.2.45",
    "chai": "^4.1.2",
    "mocha": "^4.1.0",
    "nodemon": "^1.14.7",
    "ts-node": "^4.1.0",
    "typescript": "^2.6.2"
  },
  "nodemonConfig": {
    "ext": "ts"
  }
}
